{
  "name": "openenv-root",
  "version": "0.1.0",
  "private": true,
  "description": "openenv monorepo — CLI + kits registry",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/cli",
    "test": "npm test --workspace=packages/cli"
  },
  "license": "MIT"
}
